name: native_support_plugin
description: Internal native support plugin for Status Saver
version: 0.0.1
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.statussaver.native_support
        pluginClass: NativeSupportPlugin

dependencies:
  flutter:
    sdk: flutter
